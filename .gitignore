# Build output
/src-tauri/target/
/src-tauri/gen/

# Credentials — these never belong in the repo. The app stores them in the
# OS config dir, but guard against a stray copy landing here.
cookies.txt
accounts.json
.ig-session
*.cookies

.DS_Store
